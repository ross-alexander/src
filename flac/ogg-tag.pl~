#!/usr/bin/perl

use 5.34.0;
# use Magic::Tag::Ogg;
use Music::Tag;

for my $filename (@ARGV)
{
    my $info = Music::Tag->new($filename, {verbose => 1});
    $info->get_tag() || die;
    printf "Artist is %s\n", $info->artist();
    printf "Album is %s\n", $info->album();
}
